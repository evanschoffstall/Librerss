/**
 * Keep this alias route dynamically evaluated while delegating the handler to
 * the canonical compatibility-check implementation.
 */
export const dynamic = "force-dynamic";

export { POST } from "../compatibility-check/route";

